<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Appointments & Meetings</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white">

  <!-- Header with Logo and Title -->
  <header class="bg-blue-900 text-white py-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center px-6">
      <div class="flex items-center space-x-4">
        <img src="/path/to/railway-logo.png" alt="Logo" class="h-12 w-12">
        <div>
          <h1 class="text-xl font-bold">पूर्व तट रेलवे</h1>
          <h2 class="text-sm font-light tracking-wider">(EAST COAST RAILWAY)</h2>
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <a href="/admin/dashboard" class="bg-gray-700 px-4 py-2 rounded text-white hover:bg-gray-800 transition">Home</a>
        <a href="/logout" class="bg-green-600 px-4 py-2 rounded text-white hover:bg-green-700 transition">Logout</a>
      </div>
    </div>
  </header>
  



  <!-- Main Form -->
  <main class="max-w-2xl mx-auto mt-10 p-6 bg-blue-50 border border-blue-300 rounded-md shadow">
    <h2 class="text-xl font-bold mb-6 text-blue-700">Appointments & Meetings</h2>

    <form action="/admin/appointments" method="POST" class="space-y-4">

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block font-semibold text-blue-900 mb-1">Appointment / Meeting Date:</label>
          <input type="date" name="date" class="w-full border rounded px-3 py-2" required>
        </div>

        <div>
          <label class="block font-semibold text-blue-900 mb-1">Appointment / Meeting Time:</label>
          <input type="time" name="time" class="w-full border rounded px-3 py-2" required>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block font-semibold text-blue-900 mb-1">Appointment / Meeting With:</label>
          <input type="text" name="withh" placeholder="Enter Person's Name" class="w-full border rounded px-3 py-2" required>
        </div>

        <div>
          <label class="block font-semibold text-blue-900 mb-1">Designation:</label>
          <input type="text" name="designation" placeholder="Enter Designation" class="w-full border rounded px-3 py-2" required>
        </div>
      </div>

      <div>
        <label class="block font-semibold text-blue-900 mb-1">Purpose:</label>
        <textarea name="purpose" placeholder="Enter Purpose" rows="3" class="w-full border rounded px-3 py-2" required></textarea>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block font-semibold text-blue-900 mb-1">Venue:</label>
          <input type="text" name="venue" value="Chintan Kaksha" class="w-full border rounded px-3 py-2" required>
        </div>

        <div class="flex items-center mt-6">
          <input type="checkbox" name="isVIP" id="vip" class="mr-2">
          <label for="vip" class="text-blue-900 font-semibold">Is VIP?</label>
        </div>
      </div>

      <div class="flex justify-center space-x-4 mt-6">
        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 transition">SUBMIT</button>
        <button type="reset" class="bg-gray-400 text-white px-6 py-2 rounded hover:bg-gray-500 transition">CLEAR</button>
      </div>
    </form>
  </main>
</body>
</html>
